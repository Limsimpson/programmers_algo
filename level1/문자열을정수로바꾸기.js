// 첫 번째 풀이
const solution = (s) => {
  return parseInt(s, 10);
};
// 두 번째 풀이
const solution = (s) => {
  return Number(s);
};
// 세 번째 풀이
// 아래의 풀이들은 동적 언어인 Javascript가 문자와 숫자를 사칙연산 할 경우 숫자를 반환하는 성격을 이용함.
const solution = (s) => {
  return s / 1;
};
// 네 번째 풀이
const solution = (s) => {
  return +s;
};
